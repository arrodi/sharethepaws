architecture: replication
auth:
  enabled: true
  existingSecret: sharethepaws-secrets
  existingSecretPasswordKey: redis-password
master:
  persistence:
    enabled: true
    size: 8Gi
replica:
  replicaCount: 1
  persistence:
    enabled: true
    size: 8Gi
