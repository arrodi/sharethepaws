name: aks-deploy-minio

on:
  workflow_dispatch:
    inputs:
      namespace:
        description: Kubernetes namespace
        required: true
        default: sharethepaws

env:
  NAMESPACE: ${{ github.event.inputs.namespace }}

jobs:
  deploy-minio:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4

      - name: Azure login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Set AKS context
        uses: azure/aks-set-context@v4
        with:
          resource-group: ${{ secrets.AZURE_RESOURCE_GROUP }}
          cluster-name: ${{ secrets.AKS_CLUSTER_NAME }}

      - name: Install Helm
        uses: azure/setup-helm@v4

      - name: Ensure namespace
        run: |
          kubectl apply -f infra/aks/k8s-namespace.yaml
          kubectl create namespace "$NAMESPACE" --dry-run=client -o yaml | kubectl apply -f -

      - name: Add Bitnami repo
        run: |
          helm repo add bitnami https://charts.bitnami.com/bitnami
          helm repo update

      - name: Deploy MinIO
        run: |
          helm upgrade --install minio bitnami/minio \
            --namespace "$NAMESPACE" \
            -f infra/aks/values-minio.yaml \
            --set auth.rootUser='${{ secrets.MINIO_ROOT_USER }}' \
            --set auth.rootPassword='${{ secrets.MINIO_ROOT_PASSWORD }}' \
            --wait

      - name: Verify MinIO
        run: |
          kubectl -n "$NAMESPACE" get pods -l app.kubernetes.io/name=minio
          kubectl -n "$NAMESPACE" get svc minio || true
